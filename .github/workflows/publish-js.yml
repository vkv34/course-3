name: publish Js App
on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Git clone repo
        uses: actions/checkout@v4
      - name: Set java version
        uses: actions/setup-java@v4
        with:
          distribution: 'oracle'
          java-version: 21
      - name: chmod
        run: chmod +x gradlew && chmod +x props.sh
      - name: publish
        env:
          FTP_SERVER: ${{secret.FTP_SERVER}}
          FTP_LOGIN: ${{secret.FTP_LOGIN}}
          FTP_PASSWORD: ${{secret.FTP_PASSWORD}}
        run: | 
          bash props.sh $FTP_SERVER $FTP_LOGIN $FTP_PASSWORD
          ./gradlew App:ComposeApp:publishJsApp --no-daemon
